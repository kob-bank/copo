apiVersion: v1
kind: Service
metadata:
  name: {{ include "copo.fullname" . }}
  labels:
    {{- include "copo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "copo.selectorLabels" . | nindent 4 }}
